services:
  rank-climber-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rank-climber-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
